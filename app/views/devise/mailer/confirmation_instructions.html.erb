<p>アカウント登録ありがとうございます。 <%= @email %>さん!</p>

<p>以下のリンクをクリックして登録完了となります。</p>

<%= link_to 'アカウントの確認', confirmation_url(@resource, {confirmation_token: @token, config: message['client-config'].to_s, redirect_url: message['redirect-url']}) %>